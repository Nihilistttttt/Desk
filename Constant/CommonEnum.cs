/// <summary>
/// 默认参数
/// </summary>
namespace GeekDesk.Constant
{
    public enum CommonEnum
    {
        WINDOW_WIDTH = 1920, //默认窗体宽度
        WINDOW_HEIGHT = 850, //默认窗体高度
        MENU_CARD_WIDHT = 10, //默认菜单栏宽度
        IMAGE_WIDTH = 92, //默认图标宽度
        IMAGE_HEIGHT = 92, //默认图标高度
        IMAGE_WIDTH_AM = 92, //动画变换宽度
        IMAGE_HEIGHT_AM = 92, //动画变换高度
        IMAGE_PANEL_WIDTH = 112, //图标容器宽度
        IMAGE_PANEL_HEIGHT = 126, //图标容器高度
        WINDOW_ANIMATION_TIME = 200, //主窗口动画时间,
    }
}
